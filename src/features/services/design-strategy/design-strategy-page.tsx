import { ServicePage } from "../shared/service-page";
import { designStrategyContent } from "./content";

export function DesignStrategyPage() {
  return <ServicePage data={designStrategyContent} />;
}
